'use strict';

var Constants = {
  // General

  // Server

  // Socket
  RECONNECT: true,
  RECONNECT_INTERVAL: 1000,
  AUTO_CONNECT: true,
  USE_QUEUE: true,
  QUEUE_SIZE: Infinity
};

module.exports = Constants;
